{% assign array_string = "mike,denise,cj,qwerty"  %}
{% assign array = array_string | split: ',' %}

<ul>
{% 	for name in array %}
  {% if name.size < 4 %}
	<li>{{name}}</li>
  {% endif%}
{% endfor %}
</ul>